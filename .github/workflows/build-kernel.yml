name: Build Kernel

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v4
        with:
          path: kernel

      - name: Checkout cross-compiler toolchain
        uses: actions/checkout@v4
        with:
          repository: LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9
          path: toolchain
          fetch-depth: 1

      - name: Write build script
        run: |
          cat > build.sh << 'SCRIPT'
          #!/bin/bash
          set -e
          apt-get update -qq
          apt-get install -y -qq --no-install-recommends \
            build-essential bc bison flex libssl-dev libncurses-dev python-is-python3 ca-certificates
          cd kernel
          export ARCH=arm64
          export ANDROID_MAJOR_VERSION=o
          export CROSS_COMPILE=/workspace/toolchain/bin/aarch64-linux-android-
          # pass as make arguments, not env vars - this kernel's Makefile
          # sets HOSTCFLAGS/KCFLAGS internally, which silently overrides
          # an exported environment variable of the same name
          MAKEFLAGS="HOSTCFLAGS=-fcommon KCFLAGS=-fcommon"
          make $MAKEFLAGS exynos7870-a2corelte_defconfig
          make $MAKEFLAGS -j$(nproc)
          SCRIPT
          chmod +x build.sh

      - name: Build kernel (Ubuntu 22.04 container + old-kernel-on-modern-gcc patches)
        run: docker run --rm -v "$GITHUB_WORKSPACE":/workspace -w /workspace ubuntu:22.04 ./build.sh

      - name: Upload kernel Image
        uses: actions/upload-artifact@v4
        with:
          name: a2corelte-kernel-Image
          path: kernel/arch/arm64/boot/Image
          if-no-files-found: error
